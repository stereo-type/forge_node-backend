# Начало работы с Forge Node

Это пошаговое руководство по первому запуску проекта Forge Node.

## Предварительные требования

Убедитесь, что у вас установлено:

- ✅ **Docker Desktop** (версия 20.10+)
- ✅ **Docker Compose** (версия 2.0+)
- ✅ **Node.js** (версия 20+) - для локальной разработки
- ✅ **pnpm** (версия 8+) - установка: `npm install -g pnpm`
- ✅ **Git** (версия 2.30+)

### Проверка установленных версий

```bash
docker --version
docker-compose --version
node --version
pnpm --version
git --version
```

## Шаг 1: Клонирование проекта

```bash
# Клонирование основного репозитория
git clone <URL-репозитория> forge-node
cd forge-node

# Если frontend настроен как submodule:
git submodule init
git submodule update

# Или клонировать сразу с submodules:
# git clone --recurse-submodules <URL-репозитория> forge-node
```

## Шаг 2: Настройка окружения

```bash
# Копируем пример конфигурации
cp env.example .env

# Открываем .env для редактирования
# Измените порты, если у вас заняты стандартные:
# - POSTGRES_PORT=5433 (вместо 5432)
# - REDIS_PORT=6380 (вместо 6379)
# - BACKEND_PORT=3001
# - FRONTEND_PORT=3000
```

### Важные переменные для изменения

```env
# Измените пароль БД для production
POSTGRES_PASSWORD=your-secure-password

# Измените JWT secret для production
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Настройте подключение к Forge Flow (управленка)
FORGE_FLOW_API_URL=http://your-forge-flow-server
FORGE_FLOW_API_TOKEN=your-token-from-forge-flow
```

## Шаг 3: Первый запуск через Docker

```bash
# Вариант 1: Использовать Makefile (рекомендуется)
make up

# Вариант 2: Напрямую через docker-compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Или только логи одного сервиса
docker-compose logs -f node-backend
```

### Ожидаемый результат

После запуска должны работать 4 контейнера:
- ✅ `forge-node-db` - PostgreSQL база данных
- ✅ `forge-node-redis` - Redis кэш и очередь
- ✅ `forge-node-backend` - NestJS API сервер
- ✅ `forge-node-frontend` - Next.js веб-приложение

## Шаг 4: Проверка работы

Откройте в браузере:

1. **Frontend**: http://localhost:3000
   - Должна открыться главная страница с названием "Forge Node"

2. **Backend Health**: http://localhost:3001/health
   - Должен вернуть JSON с `status: "ok"`

3. **Backend API**: http://localhost:3001/api
   - API endpoints доступны под этим префиксом

### Проверка через curl

```bash
# Проверка Backend Health
curl http://localhost:3001/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2024-12-14T...","environment":"development","version":"1.0.0"}
```

## Шаг 5: Остановка и очистка

```bash
# Остановить все сервисы
make down
# или
docker-compose down

# Остановить и удалить volumes (БД будет очищена!)
docker-compose down -v
```

## Локальная разработка (без Docker)

Если вы хотите разрабатывать без Docker:

### Backend

```bash
cd backend

# Установка зависимостей
pnpm install

# Запуск dev сервера (требуется PostgreSQL и Redis)
pnpm start:dev
```

### Frontend

```bash
cd frontend

# Установка зависимостей
pnpm install

# Запуск dev сервера
pnpm dev
```

**Важно**: Для локальной разработки нужно запустить PostgreSQL и Redis отдельно или через Docker:

```bash
# Запустить только БД и Redis
docker-compose up -d node-db node-redis
```

## Распространённые проблемы

### Порт уже занят

**Проблема**: `Error: bind: address already in use`

**Решение**: Измените порт в `.env` файле:
```env
POSTGRES_PORT=5434  # вместо 5433
BACKEND_PORT=3002   # вместо 3001
FRONTEND_PORT=3001  # вместо 3000
```

### Права доступа (Linux/Mac)

**Проблема**: Permission denied при работе с файлами

**Решение**: Установите корректные UID/GID в `.env`:
```bash
# Узнайте ваш UID и GID
id -u  # например: 1000
id -g  # например: 1000

# Установите в .env
UID=1000
GID=1000
```

### Backend не стартует

**Проблема**: Backend контейнер постоянно перезапускается

**Решение**: Проверьте логи:
```bash
docker-compose logs node-backend

# Возможные причины:
# 1. БД не готова - подождите 30 секунд
# 2. Ошибка в коде - проверьте логи
# 3. Нет зависимостей - пересоберите: docker-compose build node-backend
```

### Frontend показывает ошибку

**Проблема**: Frontend не может подключиться к Backend

**Решение**: Проверьте URL в `.env`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
# Убедитесь, что порт совпадает с BACKEND_PORT
```

## Следующие шаги

После успешного запуска:

1. 📖 Прочитайте [README.md](README.md) для общей информации
2. 🔧 Изучите [backend/README.md](backend/README.md) для разработки API
3. 🎨 Изучите [frontend/README.md](frontend/README.md) для разработки UI
4. 🔄 Прочитайте [SUBMODULE_SETUP.md](SUBMODULE_SETUP.md) если работаете с submodules
5. 📝 Смотрите [CHANGELOG.md](CHANGELOG.md) для списка TODO

## Получение помощи

Если у вас возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Попробуйте пересоздать контейнеры: `docker-compose down -v && docker-compose up -d --build`
4. Обратитесь к автору: evil_tut@mail.ru

## Готово! 🎉

Теперь вы можете начать разработку Forge Node!
